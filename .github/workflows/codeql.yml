# .github/workflows/codeql.yml
#
# CodeQL static analysis workflow.
#
# Forensic value: produces continuously-updated, time-stamped records
# of known vulnerabilities in source code, stored in the repository's
# Security tab. If an incident occurs, investigators can answer:
# "was this vulnerability detectable before the incident, and when?"

name: CodeQL

on:
  push:
    branches: [main, "devsecops/**"]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 7 * * 1'

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze JavaScript/TypeScript
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      security-events: write
      contents: read
      actions: read

    strategy:
      fail-fast: false
      matrix:
        language: ['javascript-typescript']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          config-file: ./.github/codeql-config.yml

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
